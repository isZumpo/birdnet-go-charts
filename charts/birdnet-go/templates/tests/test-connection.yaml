apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "birdnet-go.fullname" . }}-test-connection"
  labels:
    {{- include "birdnet-go.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "birdnet-go.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
